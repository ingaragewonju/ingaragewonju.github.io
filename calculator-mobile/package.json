{
  "name": "global-income-percentile-calculator-mobile",
  "version": "1.2.0",
  "description": "Mobile hybrid app for global income percentile calculations.",
  "main": "index.html",
  "scripts": {
    "init-capacitor": "npm install @capacitor/core @capacitor/cli @capacitor/android @capacitor/ios",
    "cap-add-android": "npx cap add android",
    "cap-add-ios": "npx cap add ios",
    "cap-sync": "npx cap sync",
    "cap-open-android": "npx cap open android",
    "cap-open-ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  }
}
